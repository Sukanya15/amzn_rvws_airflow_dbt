# packages.yml
packages:
  - package: calogica/dbt_date
    version: 0.10.0 # Use the latest stable version
  - package: dbt-labs/dbt_utils
    version: 1.1.1 # Or latest stable
  # For Great Expectations, if you want direct integration beyond dbt's native tests
  # - package: calogica/dbt_expectations
  #   version: 0.8.0 # Use the latest stable version